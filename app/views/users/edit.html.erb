<div class="form-wrapper">
  <h2 class="heading">アカウント編集</h2>
  <div class="form">
    <%= form_with model: @user , url: user_path(current_user), method: :put, local: true do |f|%>
      <%= render 'user-form.html.erb', f: f%>
    <% end %>
  </div>
</div>
